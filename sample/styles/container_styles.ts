import type { BaseStyles } from "../../src";

export const containerStyles: BaseStyles = {
    borderRadius: 80,
    borderSize: 4,
    bgColor: "gray",
    gap: 10,
    justifyContent: "center",
    alignItems: "center",
    display: "flex",
    width: 250,
};